# Dependencies
node_modules/
package.json
package-lock.json
bun.lock

# Secrets — NEVER commit
alibaba-cloud.apikey
.env
*.secret

# Ephemeral session files
session-*.md

# OS junk
.DS_Store

# Per-machine config layer (auto-merged on top of opencode.json)
opencode.jsonc

# Legacy per-machine layer (required OPENCODE_CONFIG; superseded by opencode.jsonc)
opencode.local.json

# direnv
.envrc
.direnv/
